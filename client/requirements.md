## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting (pt-BR locale)
recharts | For dashboard analytics charts
lucide-react | Already in base, but emphasizing for icons
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication uses Replit Auth (already configured in backend).
API endpoints are defined in shared/routes.ts.
Language: Portuguese (pt-BR).
Dark mode support is required.
